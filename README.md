# testes-selenium-gigancaQs
#Versao
Python 3.10

Dependencias do projeto
pip install behave
pip install selenium
pip install nose


Para rodar os teste rodar o comando behave no terminal na pasta raiz do projeto
